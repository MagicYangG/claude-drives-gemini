{
  "name": "claude-drives-gemini",
  "version": "1.1.0",
  "description": "Drive a logged-in Gemini Pro web session via CDP to generate images/video/music from Claude Code — media lands on disk, bytes never enter the agent context.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "private": true
}
